import { Locale } from ".";

const localeValues: Locale = {
    locale: 'en-US',
    global: {
        placeholder: '请选择',
    },
    Input:{
        placeholder:'place INPUT'
    }
};

export default localeValues;
