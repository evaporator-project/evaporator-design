import { Locale } from ".";

const localeValues: Locale = {
    locale: 'zh-cn',
    global: {
        placeholder: '请选择',
    },
    Input:{
        placeholder:'请'
    }
};

export default localeValues;
